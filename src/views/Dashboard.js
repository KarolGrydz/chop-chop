import React, { Fragment } from 'react';
import { AppNavbar } from './AppNavbar';

export const Dashboard = () => {
  return (
    <Fragment>
      <AppNavbar />
      <div>Dashborad</div>
    </Fragment>
  );
};
